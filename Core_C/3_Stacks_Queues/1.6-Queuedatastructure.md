# Implement a Queue data structure using an array. Include enqueue, dequeue, front, rear, and isEmpty operations. Handle queue full/empty.

```c

#include <stdio.h>
#include <stdlib.h>

#define MAX 5   

typedef struct {
    int arr[MAX];
    int front, rear;
} Queue;

// Initialize queue
void initQueue(Queue* q) {
    q->front = -1;
    q->rear = -1;
}

// Check if empty
int isEmpty(Queue* q) {
    return (q->front == -1);
}

// Check if full
int isFull(Queue* q) {
    return (q->rear == MAX - 1);
}

// Enqueue (insert element at rear)
void enqueue(Queue* q, int value) {
    if (isFull(q)) {
        printf("Queue Overflow! Cannot enqueue %d\n", value);
        return;
    }
    if (q->front == -1) q->front = 0;  // first element
    q->arr[++q->rear] = value;
    printf("Enqueued: %d\n", value);
}

// Dequeue (remove element from front)
int dequeue(Queue* q) {
    if (isEmpty(q)) {
        printf("Queue Underflow! Cannot dequeue.\n");
        return -1;
    }
    int removed = q->arr[q->front];
    if (q->front == q->rear) {  
        // last element removed
        q->front = q->rear = -1;
    } else {
        q->front++;
    }
    return removed;
}

// Get front element
int getFront(Queue* q) {
    if (isEmpty(q)) {
        printf("Queue is empty.\n");
        return -1;
    }
    return q->arr[q->front];
}

// Get rear element
int getRear(Queue* q) {
    if (isEmpty(q)) {
        printf("Queue is empty.\n");
        return -1;
    }
    return q->arr[q->rear];
}

// Display queue
void display(Queue* q) {
    if (isEmpty(q)) {
        printf("Queue is empty.\n");
        return;
    }
    printf("Queue: ");
    for (int i = q->front; i <= q->rear; i++) {
        printf("%d ", q->arr[i]);
    }
    printf("\n");
}

int main() {
    Queue q;
    initQueue(&q);

    enqueue(&q, 10);
    enqueue(&q, 20);
    enqueue(&q, 30);
    enqueue(&q, 40);
    enqueue(&q, 50);
    enqueue(&q, 60);  // should show overflow

    display(&q);

    printf("Dequeued: %d\n", dequeue(&q));
    printf("Dequeued: %d\n", dequeue(&q));

    display(&q);

    printf("Front Element: %d\n", getFront(&q));
    printf("Rear Element : %d\n", getRear(&q));

    return 0;
}

```

### Output:

```c

Enqueued: 10
Enqueued: 20
Enqueued: 30
Enqueued: 40
Enqueued: 50
Queue Overflow! Cannot enqueue 60
Queue: 10 20 30 40 50
Dequeued: 10
Dequeued: 20
Queue: 30 40 50
Front Element: 30
Rear Element : 50

```
